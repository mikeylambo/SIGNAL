{
  "name": "signal-brain-training",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:itch": "VITE_ITCH_BUILD=1 SIGNAL_ITCH_BUILD=1 npm run build && node scripts/prepare-itch-build.mjs",
    "preview": "vite preview",
    "test": "playwright test",
    "check:launch": "node scripts/check-launch.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "three": "0.128.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^25.9.3",
    "@types/three": "0.128.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
